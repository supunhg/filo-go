version: "2"

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - unconvert

linters-settings:
  staticcheck:
    checks:
      - all
      - -QF1012  # Disable "Use fmt.Fprintf instead of WriteString(fmt.Sprintf(...))"
